ES6
let命令 定义变量
   1.没有变量提升
   2.不能重复申明
   3.临时失效区（暂时性死区）
   4.块级作用域 （let声明的变量 const声明的常量  才有用）  {   let   }


const 定义常量   大写只是一种习惯不强制
json.parse讲json格式转换为对象


闭包：
一个外部函数，里面有一个内部函数，内部函数调用外部函数的局部变量，当外部函数执行完成后，局部变量不会被释放，内部函数一直在使用。
Scss
给文件名前加“_”下划线，文件就不会被编译。
@import “文件名”（ 文件名前不需要加下划线）引入文件

sass -i进入运算可加减乘除
数学函数：
abs()取绝对值  round()四舍五入 ceil()向上取整 floor()向下取整
percentage()自动换成百分比  min() 取小 max()取大
type-of()可以输出类型 颜色输出color 布尔值输出bool null输出null
字符串函数：
to-upper-case()字母小写变大写   to-lower-case()变小写
str-length()长度  str-index(abc,a)求索引，下标从1开始
str-insert() str-insert(abc,d,3)  输出"abdc"